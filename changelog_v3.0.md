# WindowsTweaks Pro — обновление до версии 3.0
**Дата: 21.02.2026 | Разработчик: Виталий Николаевич (vitalikkontr)**

---

## Что нового и исправлено в v3.0

### ➕ +16 новых твиков

**Производительность (игры и система):**
- Аппаратное ускорение GPU (HAGS) — снижает задержку в играх (Win10 2004+)
- Отключение HPET — снижает задержку ввода на некоторых системах
- Принудительное включение TRIM для SSD — продлевает срок службы накопителя
- Отключение ускорения мыши — линейный ввод для игр и графики
- Приоритет CPU для активного приложения (Win32PrioritySeparation)

**Приватность:**
- Отключение службы биометрии (Windows Hello)
- Запрет доступа приложений к камере
- Запрет доступа приложений к микрофону
- Очистка истории последних файлов при выходе
- Отключение Центра уведомлений (Action Center)

**Сеть:**
- Включение ECN — снижает потери пакетов при нагруженном канале
- Отключение алгоритма Nagle — снижает пинг в онлайн-играх

**Интерфейс (UI):**
- Отключение анимации открытия/закрытия окон
- Показывать строку меню в Проводнике (Файл, Правка, Вид...)
- Отключение динамической подсветки поиска Windows 11
- Автовключение NumLock при запуске Windows

---

### 🔧 Исправленные баги в TweakEngine

- **`DisableSearchIndexing` и `DisableWindowsSearch`** — были полным дублем (оба делали `sc stop WSearch`). Разделены: `DisableSearchIndexing` теперь дополнительно отключает политики индексирования через реестр
- **`DisableNagle`** — писал в ключ реестра `\Interfaces` (контейнер), что ничего не делало. Исправлено на `\Parameters`
- **`OptimizeTCPIP`** — использовал устаревшие команды Chimney/DCA/NetDMA, которых нет в Windows 10/11. Заменено на актуальные параметры: RSS, RSC, ECN, InitialRto
- **`ApplyAllTweaksAsync`** — не заполнял `lastOperationTweaks`, из-за чего лог результатов был пустым
- **`CreateRestorePoint`** — команда PowerShell не ждала завершения перед проверкой кода выхода
- **`ExecuteCommand`** — конфликт `UseShellExecute=true` + `CreateNoWindow=true` (в WPF эти параметры несовместимы). Заменено на `WindowStyle.Hidden`
- **`RevertAllTweaksAsync`** — не очищал `enabledTweaks` при полном откате, из-за чего при следующем нажатии «Применить» снова предлагались уже отменённые твики
- **Синхронизация состояния при старте** — при запуске `enabledTweaks` был пуст, и кнопка «Отменить» предлагала откатить все ранее применённые твики

---

### 🎨 Исправления интерфейса (MainWindow)

- **Кнопка закрытия диагностического окна** — `Grid.SetColumn(closeBtn, 2)` при двух колонках (0 и 1). Кнопка была невидима, исправлено на колонку 1
- **`NavigationList.SelectedIndex = 0`** не устанавливался при старте — первый пункт не подсвечивался активным
- **Грамматика** — «Классический контекстное меню» → «Классическое контекстное меню»
- **Описание `OptimizeTCPIP`** обновлено (убраны упоминания Chimney/DCA)
- **Tooltip `DisableDefender`** — исправлены опечатки («приминением», «антивируснке», «зашиты»)
- **Версия и количество твиков** в «О программе» обновлены до актуальных значений

---

### 🗂 Исправления контекстных меню (DesktopContextMenu / ComputerContextMenu)

- **«Темы» и «Фон рабочего стола»** открывали одно и то же окно (`pageWallpaper`). «Темы» теперь открывает главный раздел персонализации
- **`GetDiagnosticInfo` в DesktopContextMenu** — был захардкожен (всегда показывал одно и то же). Теперь реально читает реестр и показывает что установлено
- **Переменная `settings`** в `App.xaml.cs` объявлялась но нигде не использовалась
- **Логика флага первого запуска** — флаг не создавался если пользователь нажимал «Нет». Исправлено: флаг создаётся сразу независимо от ответа
- **Python-синтаксис `{'=' * 60}`** в `App.xaml.cs` — компиляционная ошибка. Заменено на `new string('=', 60)`
- **23 двойные точки с запятой `;;`** убраны по всему `MainWindow.xaml.cs`
- **ASCII-рамки `╔═══╗`** заменены на единый стиль `◆` / `────` во всех файлах

---

*Всего исправлено багов: 20+ | Новых твиков: 16 | Итого твиков в программе: 81*
